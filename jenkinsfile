pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh '''
                cd /var/jenkins_home/workspace/jnkins 
                docker-compose up -d
                '''
            }
        }
    }
}
